export { auth as middleware } from "@/auth"

export const config = {
  matcher: [
    "/profil",
    "/cv",
    "/parcours",
    "/forum",
    "/conseil",
    "/stages",
    "/bourses",
    "/ressources",
    "/calendrier",
    "/entrepreneuriat",
    "/success-stories",
  ],
}
